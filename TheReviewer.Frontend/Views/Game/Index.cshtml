@using TheReviewer.Frontend.Models
@using TheReviewer.Logic.Models
@model MediaViewModel

@{
    ViewData["Title"] = "Game Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Games</h1>

<p>
    <a asp-controller="Review" asp-action="CreateGame" class="btn btn-primary">Create New</a>
</p>

<table class="table table-striped">
    <thead>
        <tr>
            <th>Game title</th>
            <th>Publisher</th>
            <th>Avg score</th>
            <th># Reviews</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
        @if (Model?.Media != null && Model.Media.Any())
        {
            foreach (var game in Model.Media)
            {
                var reviewCount = Model.Reviews?.Count(r => r.MediaId == game.Id) ?? 0;
                var averageScore = Model.GetAverageScore(game.Id);
                <tr>
                    <td>
                        <a asp-controller="Media" asp-action="Details" asp-route-id="@game.Id" asp-route-mediaTypeId="@Model.MediaTypeId">
                            @game.Name
                        </a>
                    </td>
                    <td>@game.Publisher</td>
                    <td>@averageScore</td>
                    <td>@reviewCount</td>
                    <td>
                        <a asp-controller="Review" asp-action="CreateGame">Add review</a>
                    </td>
                </tr>
            }
        }
        else
        {
            <tr><td colspan="5">No games found.</td></tr>
        }
    </tbody>
</table>
